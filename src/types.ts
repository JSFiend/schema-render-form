export const SCHEMA_TYPES = Object.freeze({
  ARRAY: 'array',
  BOOLEAN: 'boolean',
  INTEGER: 'number',
  NUMBER: 'number',
  OBJECT: 'object',
  STRING: 'string',
  NULL: null,
});

export const INPUT_TYPES = Object.freeze({
  CHECKBOX: 'checkbox',
  EMAIL: 'email',
  HIDDEN: 'hidden',
  NUMBER: 'number',
  RADIO: 'radio',
  SELECT: 'select',
  SWITCH: 'switch',
  TEXT: 'text',
  TEXTAREA: 'textarea',
  URL: 'url',
});
